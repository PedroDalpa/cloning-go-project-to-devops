name: CI

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: 'Build and Push'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate SHA
        id: generate_sha
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            pedrodalpa/nlw.journey.api:${{steps.generate_sha.outputs.sha}}
            pedrodalpa/nlw.journey.api:latest

      # - name: Build docker image
      #   run: docker build -t pedrodalpa/nlw.journey.api:${{steps.generate_sha.outputs.sha}} .

      # - name: Push docker image
      #   run: |
      #     docker push pedrodalpa/nlw.journey.api:${{steps.generate_sha.outputs.sha}}
      #     docker tag pedrodalpa/nlw.journey.api:${{steps.generate_sha.outputs.sha}} pedrodalpa/nlw.journey.api:latest
      #     docker push pedrodalpa/nlw.journey.api:latest
